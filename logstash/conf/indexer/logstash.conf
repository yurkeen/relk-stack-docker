input {
    redis {
        host => "redis"
        type => "redis-input"
        data_type => "list"
        key => "logstash"
    }
    syslog { 
        type => "syslog" 
        port => "5544" 
    } 
}
output {
    stdout {}
    elasticsearch {
        cluster => "logstash"
    }
} 
